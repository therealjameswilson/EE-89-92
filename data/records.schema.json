{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FRUS Eastern Europe Compiler Cue",
  "type": "object",
  "required": [
    "id",
    "date",
    "sortDate",
    "type",
    "title",
    "countries",
    "chapter",
    "status",
    "sourceFamily",
    "sourceNote",
    "nextAction",
    "topics"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable local identifier, preferably country-date-topic."
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "sortDate": {
      "type": "string",
      "format": "date"
    },
    "type": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "dateLine": {
      "type": "string"
    },
    "subjectLine": {
      "type": "string"
    },
    "countries": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "chapter": {
      "type": "object",
      "required": ["number", "name"],
      "properties": {
        "number": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5
        },
        "name": {
          "type": "string",
          "enum": [
            "Poland",
            "Hungary",
            "Czechoslovakia",
            "Romania, Bulgaria, and Albania",
            "Yugoslavia and Regional"
          ]
        }
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "Harvest target",
        "Cross-volume overlap",
        "Gap check",
        "Declassification priority",
        "Source folder found",
        "Document found"
      ]
    },
    "releaseStatus": {
      "type": "string"
    },
    "coverageRole": {
      "type": "string"
    },
    "participants": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "naid": {
      "type": "string"
    },
    "catalogUrl": {
      "type": "string",
      "format": "uri-reference"
    },
    "pdfUrl": {
      "type": "string",
      "format": "uri-reference"
    },
    "sourceFamily": {
      "type": "string"
    },
    "pageCount": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of pages in the linked official PDF scan when counted. For source folders, this is the full folder scan, not a trimmed document excerpt."
    },
    "sourceNote": {
      "type": "string"
    },
    "nextAction": {
      "type": "string"
    },
    "topics": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "links": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["label", "url"],
        "properties": {
          "label": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri-reference"
          }
        }
      }
    }
  }
}
